# Comandos úteis:

python --version -> Retorna a versão do python
python -m venv venv -> Cria a venv(biblioteca)

#### 2. Ativar a Venv no terminal
.\venv\Scripts\Activate.ps1
 
#### 3. Instalar bibliotecas na venv
pip install -r requirements.txt
 